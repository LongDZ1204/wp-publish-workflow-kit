# CLAUDE.local.md — credential cục bộ, KHÔNG commit

Copy file này thành `CLAUDE.local.md` ở thư mục gốc kit rồi điền giá trị thật.
`CLAUDE.local.md` đã nằm trong `.gitignore` nên không bao giờ lên Git.
Mỗi site một khối — copy thêm khối nếu có nhiều site.
Script khớp site theo tên trong heading HOẶC domain trong URL (không phân biệt hoa/thường).
Dùng user role Editor riêng, không dùng Administrator.

### ten-ngan WordPress (REST API)
- URL: https://domain.com
- User: wp-publish
- App Password: xxxx xxxx xxxx xxxx xxxx xxxx
